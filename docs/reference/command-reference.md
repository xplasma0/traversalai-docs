# Command Reference

## Core Groups

- `gateway`: lifecycle and runtime state
- `dashboard`: control UI access
- `devices`: pairing and token operations
- `status`: consolidated health output

Use `--help` per command for exact options.
